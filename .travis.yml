sudo : required
services:
  - docker

before_install:
  - docker build -t spine/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true spine/react-test npm test -- --coverage

after_success:
  - docker build -t spine/multi-client ./client
  - docker build -t spine/multi-nginx ./nginx
  - docker build -t spine/multi-server ./server
  - docker build -t spine/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push spine/multi-client
  - docker push spine/multi-nginx
  - docker push spine/multi-server
  - docker push spine/multi-worker